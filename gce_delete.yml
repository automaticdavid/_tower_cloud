- name: create gce instances  
  hosts: "{{ gce_ins_name }}"
  connection: local
  gather_facts: False

  tasks:

    - name: Delete a set of instances
      gce:
        instance_names: "{{play_hosts}}"
        zone: europe-west1-b
        state: absent
        delegate_to: localhost







