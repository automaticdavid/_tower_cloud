---

- name: App actions
  hosts: "*{{ ec2_scope }}"
  gather_facts: yes
  become: yes
  user: ec2-user

  tasks:

    - name: Insight Register
      command: redhat-access-insights --register

      